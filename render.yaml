services:
  - type: web
    name: signalboard-analytics
    env: docker
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_BASE_URL
        value: https://router.huggingface.co/v1
      - key: JWT_SECRET
        generateValue: true
