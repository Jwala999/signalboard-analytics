services:
  - type: web
    name: signalboard-analytics
    env: docker
    plan: free
    dockerCommand: npm run start
    envVars:
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        generateValue: true
      - key: OPENAI_API_KEY
        sync: false
